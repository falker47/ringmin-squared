# TASK LOG - Global Four-Prefix Optimization

Append-only. Add a new entry to correct previous information.

## 2026-07-17 - Startup And Expected Delta

- Read the repository contract, project memory, current status, the direct
  four-prefix theorem, the all-\(n\) summary, its prior dossier, and the
  preceding ordered-weight/simplex optimizations.
- Confirmed a clean `main` worktree at
  `24bea121d96906bfb884c57e3e97f189351d8791`.
- Classified the task as STRICT and fixed the protected scope: no finite
  rounding, five-prefix extension, production or artifact work, or
  enumeration-limit change.
- Defined the expected delta as one exact global four-prefix compact-closure
  theorem, a complete branch/face audit, one independent exact algebraic
  diagnostic, and authoritative-source synchronization.

## 2026-07-17 - Exact Reduction And Global Algebra

- Reduced each ordered weight independently to its clipped optimum. Monotonicity
  of the clipping map makes the ordered-weight constraints automatic and leaves
  a compact ordered density problem with fifteen regimes
  \(H^hM^m0^{4-h-m}\).
- Solved the fixed-\(\alpha\) Bellman problem. The initial derivation correctly
  identified all transition data, but an independent audit found that its
  high-variable derivative formula had been applied across an `HM` interface
  where it is valid only for `HH`.
- Corrected the proof by differentiating the three exact `HM` base maps and
  the `H0` base separately, proving derivative greater than one on every
  relevant interval, and using the high-variable formula only for the
  subsequent `HH` induction. A second read-only audit confirmed the corrected
  completeness argument.
- Established the winning sequence `0000`, `MMMM`, `HMMM`, `HHMM`, `HHHM`,
  with the formal `HHHH` transition beyond \(\alpha=1\). Recorded the exact
  transition densities and weights.
- Classified density collisions, unused segments, internal weight diagonals,
  outer weight facets, and the \(\alpha=0,1\) faces. All boundary maxima are
  strictly below the unique interior value; the effective three-prefix faces
  have exact maximum \(C_{3,*}\).
- Specialized the normalized simplex certificate at four prefixes, derived
  the exact cubic envelope, isolated its unique maximizing density, recovered
  all eight remaining parameters, and evaluated the exact quadratic-surd
  coefficient. Proved \(C_{3,*}<2767/10000<C_{4,*}\) by exact integer
  comparisons and also by a structural strict extension of the three-prefix
  point.

## 2026-07-17 - Independent Diagnostic

- Added `exact_diagnostic.py`, using only the Python standard library and no
  project, test, production, artifact, or historical-oracle imports.
- The first run exposed only a diagnostic implementation error: `zip(...,
  strict=True)` was used on adjacent lists of unequal length. Corrected it and
  retained the failure here as evidence.
- A diagnostic audit then identified missing end-to-end links between the
  simplex value, cubic envelope, original objective, and reported surd. Added
  those exact identities, the three clipping-gap factorizations, both clipping
  joins, transition weights, collision reductions, primitive/irreducible
  polynomial checks, conjugate exclusion, and assertion-stripping refusal.
- The strengthened diagnostic passes and its runtime fell from roughly eleven
  seconds to roughly one and a half seconds after replacing an unnecessary
  four-dimensional weight grid with the exact separable gap factorizations.
- Ruff initially requested one mechanical reformat; formatting, lint, and the
  subsequent format check pass.

## 2026-07-17 - Authoritative Synchronization And Verification

- Extended the primary proof in `research/FIXED_ORDER_CYCLE_RATIO.md` and
  synchronized `research/ALL_N_LOWER_BOUND.md`, `start.md`,
  `PROJECT_KNOWLEDGE.md`, `CURRENT_STATUS.md`, and the research roadmap.
- Kept the task within its protected scope: no finite rounding, five-prefix
  theorem, production/test code, API, artifact, schema, backend, certificate,
  enumerator, or enumeration-limit change.
- Ran the complete 283-test suite, focused 101-test proof-note regression,
  checked-artifact semantic verification, schema-selection tests, both
  historical independent four-prefix diagnostics, Ruff, Markdown structural
  checks, exact-value consistency searches, protected-path review, full diff
  inspection, `git diff --check`, and final worktree inspection.
- Three independent read-only audits covered the mathematical proof, the
  standalone diagnostic, and cross-source consistency. Their actionable
  findings were corrected and rechecked.
- Set task status to `READY_FOR_REVIEW`; the user retains manual commit control.
