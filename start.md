# Deprecated compatibility pointer

`start.md` is no longer a substantive project source. Read `AGENTS.md` for
the operating contract, `PROJECT_KNOWLEDGE.md` for stable results, and
`CURRENT_STATUS.md` for the current task state.
